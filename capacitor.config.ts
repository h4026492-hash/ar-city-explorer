import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harshith.dallasar',
  appName: 'AR City Explorer',
  webDir: 'dist/ar-city-explorer/browser'
};

export default config;
